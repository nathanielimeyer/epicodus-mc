import { IncarceratedPipe } from './incarcerated.pipe';

describe('IncarceratedPipe', () => {
  it('create an instance', () => {
    const pipe = new IncarceratedPipe();
    expect(pipe).toBeTruthy();
  });
});
